作业要求：
    1. 用户输入帐号密码进行登陆
    2. 用户信息保存在文件内
    3. 用户密码输入错误三次后锁定用

实现方法：
    1、读取文件信息，把文件内容转成字典：normal_users和lock_users
    2、让用户输入的用户名和密码
    3、判断用户名的状态：锁定、不存在（新用户）、存在（老用户）
    4、根据用户名的状态进行下一步操作：
        锁定  ：直接退出
        不存在：提示注册，注册成功存入normal_users,重新输入用户名、密码
        存在：判断密码是否正确，正确显示登录成功信息，错误则统计次数，错误	次数达到3次，锁定该用户，存入 lock_users




